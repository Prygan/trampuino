#ifndef OBSTACLE_H
#define OBSTACLE_H

class Obstacle {
private :

   int x, y, length ;

public :

   Obstacle(int x, y, length);

   void deplace(int speed);

   //draw Obstacle
   void draw(void);

   int getX(void);

   int getY(void);

   int getLength(void);

}

#endif